[project]
name = "memory.fm-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "memory-fm",
    "streamlit==1.52.2",
]

[tool.uv.sources]
memory-fm = { git = "https://github.com/shsiddhant/memory.fm.git" }
